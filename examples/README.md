# Examples

This directory contains a buildable demo Lean project plus archival runbook prompts. They are examples only: the three skills under `skills/`, the runtime under `autoform/`, and the LSP/REPL services under `servers/` are the maintained plugin surfaces.
