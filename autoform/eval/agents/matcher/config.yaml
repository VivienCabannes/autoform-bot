model: Opus 4.6
tools:
  servers: [filesystem]
  allowlist:
    - read_text_file
    - file_grep
    - search_files
    - list_directory
max_turns: 1000
inference:
  cache:
    system: true
    messages: true
    tools: true
