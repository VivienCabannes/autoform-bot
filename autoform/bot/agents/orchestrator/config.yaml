model: Opus 4.6
max_turns: 100000
context_window: 1000000
compact_threshold: 0.85
tool_timeout_s: 120
tools:
  servers: [filesystem, dag-store, todo]
skills:
  prompt: [project/project_rubric]
inference:
    cache:
      # Anthropic: explicit cache breakpoints
      system: true
      messages: true
      tools: true
