model: Haiku 4.5
max_turns: 15
tool_timeout_s: 60
tools:
  servers: [filesystem]
inference:
    cache:
      # Anthropic: explicit cache breakpoints
      system: true
      messages: true
      tools: true
