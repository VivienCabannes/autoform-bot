model: Opus 4.6
context_window: 1000000
compact_threshold: 0.85
max_turns: 250
tool_timeout_s: 300
tools:
  servers: [lsp, lean_repl, filesystem, mathlib, git]
skills:
  prompt: [project/project_rubric]

inference:
    cache:
      # Anthropic: explicit cache breakpoints
      system: true
      messages: true
      tools: true
