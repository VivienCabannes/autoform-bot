model: Opus 4.6
context_window: 1000000
compact_threshold: 0.85
max_turns: 40
tool_timeout_s: 120
tools:
  servers: [lsp, filesystem, git]
inference:
    cache:
      system: true
      messages: true
      tools: true
