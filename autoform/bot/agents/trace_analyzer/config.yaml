model: Opus 4.6
context_window: 1000000
compact_threshold: 0.85
max_turns: 100000
tool_timeout_s: 120
tools:
  servers: [filesystem, trace-inspector]
inference:
    cache:
      # Anthropic: explicit cache breakpoints
      system: true
      messages: true
      tools: true
