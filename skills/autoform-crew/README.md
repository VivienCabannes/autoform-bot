# autoform-crew

Orchestration guide for parallel formalization with subagent teams. Tells the main thread when and how to delegate to autoform-worker, autoform-reviewer, and autoform-reader for batch proving, parallel review, and wave-based chapter formalization.

## Usage

```
/autoform-crew
```
